export const APK_DROPDOWN = {
  root: ".project-detail-apk-dropdown",
  trigger: ".project-detail-apk-dropdown__trigger",
  menu: ".project-detail-apk-dropdown__menu",
  option: ".project-detail-apk-dropdown__option",
  triggerText: ".project-detail-apk-dropdown__trigger-text",
} as const;

export const APK_FLOAT_MENU = {
  gapPx: 4,
  maxHeightPx: 280,
  minHeightPx: 120,
  minWidthPx: 200,
  flipBelowPx: 140,
  zIndex: 9999,
} as const;

export const APK_PICKER_DOM_IDS = {
  trigger: "apk-variant-trigger",
  listbox: "apk-variant-listbox",
  pickerRoot: "project-apk-picker",
  downloadLink: "download-apk-link",
  downloadTag: "download-apk-tag",
  downloadSize: "download-apk-size",
} as const;
